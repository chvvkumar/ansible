---
  - hosts: ubuntu
    become: yes
    roles:
      - {role: ubuntu_hosts}
      - {role: pihol_update}
    tags:
      - all

# ansible-playbook -i inventories/linux all.yml --limit 'all:!pi.hole'
